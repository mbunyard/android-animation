<?xml version="1.0" encoding="utf-8"?>
<objectAnimator xmlns:android="http://schemas.android.com/apk/res/android"
                android:interpolator="@android:anim/accelerate_interpolator"
                android:duration="2000"
                android:propertyName="alpha"
                android:valueFrom="0.0"
                android:valueTo="1.0" />

    <!--
        Could add interpolator with following attribute on "objectAnimator" element:
        android:interpolator="@android:anim/accelerate_interpolator"
    -->

    <!--
        Could add many "objectAnimator" elements wrapped in a "set" element.
    -->

    <!--
        Can sequence objectAnimator elements with a set, using "android:ordering" attribute
        on "set" element, but more fine-grained control of sequencing can be done in code.
    -->

